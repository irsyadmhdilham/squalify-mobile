export interface pointScore {
  pk: number;
  name: string;
  profile_image: string;
  point: number;
  designation: string;
};

export interface salesScore {
  pk: number;
  name: string;
  profile_image: string;
  amount: string;
  designation: string;
};